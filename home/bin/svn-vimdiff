#!/usr/bin/env bash

# a wrapper for using vimdiff in `svn diff`
# Option1:
#     svn diff --diff-cmd svn-vimdiff some-file
# Option2 (global):
#     in file `$HOME/.subversion/config`
#     diff-cmd = svn-vimdiff

vimdiff ${6} ${7}
